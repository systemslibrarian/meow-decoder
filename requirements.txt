# Meow Decoder - Python Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies
Pillow>=10.3.0              # Image processing (QR, GIF, PNG) - fixes GHSA-3f63-hfp8-52jq, GHSA-44wm-f244-xhp3, GHSA-j7hp-h8jx-5ppr
qrcode>=7.4.2               # QR code generation
pyzbar>=0.1.9               # QR code reading
opencv-python>=4.8.1.78     # Webcam capture and image processing - fixes GHSA-qr4w-53vh-m672 (libwebp CVE-2023-4863)
numpy>=1.24.0               # Numerical operations

# Cryptography
cryptography>=43.0.1        # AES-256-GCM, HKDF, X25519 - fixes GHSA-3ww4-gg4f-jr7f, GHSA-h4gh-qq45-vh27, and 5 other advisories
argon2-cffi>=23.1.0         # Argon2id key derivation
pynacl>=1.6.2               # Ed25519↔X25519 conversion (spec v1.2+) - fixes GHSA-mrfv-m5wm-5w6w

# Optional enhancements (for security features)
scipy>=1.11.0               # For advanced steganography (optional)
psutil>=5.9.0               # Memory monitoring for streaming (optional)
dearpygui>=1.10.0           # GUI dashboard (optional)
# liboqs-python>=0.9.0      # Post-quantum crypto (experimental, not yet stable)

# Development tools (optional)
pytest>=7.4.0               # Testing framework
black>=24.3.0               # Code formatting - fixes GHSA-fj7x-q9j7-g6q6 (ReDoS)
mypy>=1.5.0                 # Type checking
flake8>=6.1.0               # Linting

# Platform-specific notes:
# - pyzbar requires zbar library:
#   - Ubuntu/Debian: sudo apt-get install libzbar0
#   - macOS: brew install zbar
#   - Windows: Download from http://zbar.sourceforge.net/
#
# - opencv-python might need additional libraries:
#   - Ubuntu/Debian: sudo apt-get install libgl1-mesa-glx
