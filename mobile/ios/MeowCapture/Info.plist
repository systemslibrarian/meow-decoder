<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Info.plist — MeowCapture iOS configuration

  SECURITY NOTES:
  - NSCameraUsageDescription: Required, explains why camera access is needed.
  - NO NSMicrophoneUsageDescription: Microphone access is never requested.
  - NO NSLocationWhenInUseUsageDescription: Location is never accessed.
  - NO NSPhotoLibraryUsageDescription: Photo library is never accessed.
  - No App Transport Security exceptions — no HTTP/network access at all.

  The app operates in a no-network, no-microphone, camera-only mode per the air-gap
  security model. All captured data stays on device until explicitly exported to Files.
-->
<plist version="1.0">
<dict>
    <!-- App identity -->
    <key>CFBundleDevelopmentRegion</key>
    <string>en</string>
    <key>CFBundleDisplayName</key>
    <string>Meow Capture</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>1.0</string>
    <key>CFBundleSignature</key>
    <string>????</string>
    <key>CFBundleVersion</key>
    <string>1</string>

    <!-- Camera permission — required for QR scanning -->
    <key>NSCameraUsageDescription</key>
    <string>Meow Capture uses the camera to scan animated QR codes from an air-gapped computer. No images are stored or transmitted.</string>

    <!-- Supported orientations — lock to portrait for stable scanning posture -->
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
    </array>
    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <!-- React Native requirements -->
    <key>UIRequiresPersistentWiFi</key>
    <false/>
    <key>UIViewControllerBasedStatusBarAppearance</key>
    <false/>
    <key>NSAppTransportSecurity</key>
    <dict>
        <!-- Explicitly block all HTTP connections — this app never needs network access -->
        <key>NSAllowsArbitraryLoads</key>
        <false/>
    </dict>

    <!-- App Transport Security — no exceptions. Air-gapped. -->
    <key>LSRequiresIPhoneOS</key>
    <true/>

    <!-- Deep-link URL scheme: meow://capture?session_id=...&expected_frames=... -->
    <key>CFBundleURLTypes</key>
    <array>
        <dict>
            <key>CFBundleURLSchemes</key>
            <array>
                <string>meow</string>
            </array>
            <key>CFBundleURLName</key>
            <string>com.meowcapture</string>
        </dict>
    </array>

</dict>
</plist>
