"""
⚠️  DEPRECATED — DO NOT USE

This file is a legacy artifact from before the migration to pyproject.toml.
The canonical package configuration is in /pyproject.toml at the repo root.

Delete this file once confirmed nothing depends on it:
    rm meow_decoder/setup.py

See: https://setuptools.pypa.io/en/latest/userguide/pyproject_config.html
"""

raise SystemExit(
    "ERROR: meow_decoder/setup.py is deprecated. "
    "Use `pip install -e .` with the root pyproject.toml instead."
)
