"""
Archived test files — not collected by pytest.

These tests correspond to modules moved to meow_decoder/_archive/.
They are preserved for reference but are not part of the production test suite.
"""

import sys

# Prevent pytest from collecting anything in this directory
collect_ignore_glob = ["*.py"]
