"""
Tests have been split into three focused files for parallel CI execution:
  - test_invariants_critical.py (batch 1) — TestCriticalInvariants
  - test_invariants_fail_closed.py (batch 2) — TestFailClosedBehavior
  - test_invariants_regressions.py (batch 3) — TestNoRegressions

This file is intentionally empty to avoid duplicate test collection.
"""
